
# NOTE (next session): run as: python poll_coro2.py <pid> 60
# with `-u` flag for unbuffered output: python -u poll_coro2.py <pid> 60
# and coordinate with the user to send a message during the window.
# The catch dumps real_coro_N_st{state}.bin (0x510 bytes) + real_exec_N.bin (0x60).
